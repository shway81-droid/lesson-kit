---
name: lesson-kit
description: 초등 교과서 전체 PDF에서 한 차시를 뽑아 동기유발 영상·수업 슬라이드·형성평가(학생 배부본+정답지)·인포그래픽을 만든다. 형성평가 학생 배부본도 NotebookLM 인포그래픽으로 디자인한다. 내용과 구조는 에이전트가 확정하고 NotebookLM은 디자인만 담당한다. "3단원 2차시 자료 만들어줘", "차시 수업자료", "동기유발 영상 만들어줘" 같은 요청에 사용한다.
---

# lesson-kit

## 시작하기 전에

설치와 준비물은 `README.md` 를 본다 — 의존성, `notebooklm login`, 교과서·진도표 위치,
성취기준 데이터. **처음 쓰는 사람에게는 README 를 먼저 안내한다.**

이 파일은 절차다. 규칙마다 왜 그런지가 실측 근거와 함께 붙어 있으므로,
규칙을 바꾸려면 그 근거를 먼저 읽는다.

## 절대 원칙

**수업 슬라이드는 교과서 차시 PDF 를 소스로 준다. 나머지 넷은 내가 쓴 원고를 소스로 준다.**

슬라이드에 교과서를 직접 주는 이유는 실측으로 확인됐다(2026-09-07, 사회 5-2 02차시).
같은 차시를 원고 소스와 교과서 소스로 각각 만들어 비교했더니, 교과서 소스 쪽이 교과서 문장을
글자 그대로 옮겼다 — 박물관 이름, 유물 설명, 확인해요 문항의 `( , )` 답란, 활동 발문까지
원본대로였다. 재구성은 일어나지 않았다. 반면 원고 경로는 내가 한 번 옮겨 쓰는 만큼
빠지거나 달라질 여지가 있다(직전 차시에서 교과서에 없는 정의를 4개 지어낸 적이 있다).

**슬라이드 장수는 지정하지 않는다.** NotebookLM 이 교과서 분량에 맞춰 정하게 둔다.

나머지 넷(동기유발·형성평가 정답지·형성평가 학생 배부본·개념 인포그래픽)은 교과서에 그대로
있는 형태가 아니라 내가 만들어야 하는 것이므로 원고를 쓴다. `output/<차시>/00_원고/` 의
마크다운이 그 넷의 정답 원본이고, 대조는 이것을 기준으로 한다.
슬라이드의 대조 기준은 교과서 차시 본문 자체다.

**생성마다 `-s` 로 소스를 하나만 지정한다.** 한 노트북에 교과서 PDF 와 원고가 함께 들어가므로
이것을 빠뜨리면 원고 자료에 교과서 내용이 섞인다.

모든 파이썬 실행에 `PYTHONUTF8=1` 을 붙인다.

## 산출물 이름 규칙

차시 폴더 안의 파일은 **쓰임으로 부른다.** 만든 도구 이름(인포그래픽 등)을 쓰지 않는다.
같은 도구로 만든 두 자료가 있어서 도구 이름으로는 구분이 안 되기 때문이다.

| 파일 | 무엇 |
|---|---|
| `01_차시.pdf` | 분할된 교과서 차시 PDF |
| `02_동기유발.mp4` | 수업 도입 동기유발 영상 |
| `03_수업슬라이드.pdf` / `.pptx` | 수업에 쓸 슬라이드 |
| `04_형성평가.png` | 학생 배부본 문제지 |
| `04_형성평가_정답.pdf` | 교사 정답지 |
| `04_형성평가_예비.pdf` | 배부본 예비 (인포그래픽이 막히거나 흑백 인쇄가 필요할 때) |
| `05_개념정리.png` | 개념 정리·게시용 자료 |
| `99_대조리포트.md` | 대조 결과 |

훼손으로 판정해 버린 판은 `_v1_훼손` 처럼 꼬리를 붙여 남긴다. 무엇이 왜 틀렸는지가
다음 차시의 프롬프트 근거가 된다.

## 1단계 — 차시 추출

**진도표(xlsx)가 있으면 그것을 먼저 쓴다.** 출판사가 주는 나이스 업로드용 진도표에는
단원·소단원·학습내용·교과서 쪽수가 확정값으로 들어 있다. 표식 탐지는 추정이라
출판사 조판에 따라 놓친다(실측: 천재교과서 5-2 는 진도표 48차시인데 표식은 35건).

```bash
PYTHONUTF8=1 python scripts/read_progress_plan.py <진도표.xlsx> \
  --unit 1 --verify <교과서.pdf>
```

`--verify` 는 각 차시 첫 쪽의 앞머리를 함께 찍는다. **제목과 본문이 맞는지 반드시 눈으로
확인한다** — 진도표의 쪽 번호는 교과서에 인쇄된 번호이고 PDF 안 순번과 다르다. 그 차이가
`--offset` 이고 기본값은 1 이다(실측: 천재교과서 5-2 는 1 이 맞았다). 앞표지 장수가 다르면
어긋나므로 확인 없이 쓰지 않는다.

쪽수가 같은 연속 차시(2차시짜리 수업)는 자동으로 하나로 합친다. 합치지 않으면 같은 범위로
노트북을 두 번 만들어 레이트리밋을 두 배로 쓴다. 원본 그대로 보려면 `--no-merge`.

교과서와 진도표는 `textbook/<학기>/` 에 학기별로 둔다(예: `textbook/5-2/`). 출판사 저작물이라
깃에는 올리지 않는다. `textbook/README.md` 에 폴더 규칙과 학기별 `--offset` 확인 결과를 적어 둔다.

**학기 전체를 미리 잘라 둘 수도 있다.** 차시를 고를 때마다 182쪽을 다시 열지 않아도 되고,
어떤 차시가 있는지 파일 목록으로 바로 보인다.

```bash
PYTHONUTF8=1 python scripts/split_semester.py <교과서.pdf> <진도표.xlsx>   --out textbook/<학기>/차시
```

자르기 전에 **전 차시의 범위를 먼저 검사한다.** 40번째에서 범위를 벗어나면 앞의 39개는 이미
저장된 뒤라 어디까지 믿을 수 있는지 알 수 없기 때문이다. 텍스트 레이어가 없는 차시는
`[비전 필요]` 로 표시된다.

진도표가 없으면 표식으로 후보를 찾는다.

```bash
PYTHONUTF8=1 python scripts/split_lesson.py <교과서.pdf> --starts
```

표식은 공백을 지운 텍스트에 맞춘다. 교과서는 제목을 자간을 벌려 조판하므로 PDF 에서 뽑으면
`생 각 을 열 어 요` 처럼 글자마다 공백이 낀다. 새 출판사에서 0건이 나오면 그 교과서가 쓰는
제목 표식을 찾아 `LESSON_START_PATTERNS` 에 공백 없는 형태로 추가한다.

범위를 정해 잘라낸다. 쪽 번호는 0-indexed 이고 끝 쪽을 포함한다.

```bash
PYTHONUTF8=1 python scripts/split_lesson.py <교과서.pdf> \
  --from-page 32 --to-page 35 --out "output/<차시 slug>"
```

**사람 확인 지점.** 잘라낸 범위가 맞는지 물어보고 진행한다.

`비전 필요: True` 로 나오면 텍스트 레이어가 없는 PDF 다. 함께 생성된 `page_*.png` 를
Read 도구로 읽어 본문을 파악한다.

## 2단계 — 성취기준 조회

```bash
PYTHONUTF8=1 python scripts/find_standards.py \
  --text "<차시 본문>" --subject 사회 --grade-group 초5-6
```

**사람 확인 지점.** 후보를 제시하고 어느 성취기준으로 갈지 확인받는다.

## 3단계 — 원고 작성 (내가 쓴다)

차시 본문 + 확정된 성취기준(본문·해설·적용 시 고려사항)을 재료로
`output/<차시>/00_원고/` 에 4개를 쓴다. **슬라이드 원고는 쓰지 않는다** — 교과서 PDF 를 그대로 준다.

| 파일 | 내용 |
|---|---|
| `01_동기유발.md` | 1~2분 분량 영상 대본. 학생의 흥미를 끄는 질문으로 시작 |
| `03_형성평가.md` | 빈칸 문항. 정답은 `## 정답` 절로 분리 |
| `03_형성평가_학생용.md` | 위에서 `## 정답` 절을 뺀 것 + `학년 반 / 이름` 칸. **학생 배부본 소스** |
| `04_개념정리.md` | 핵심 개념과 관계를 구조적으로. **공백 뺀 250자 안팎** |

`03_형성평가_학생용.md` 는 손으로 다시 쓰지 않는다. `03_형성평가.md` 를 `## 정답` 에서 잘라
머리말만 붙인다. 두 파일의 문항이 갈라지면 정답지와 문제지가 어긋난다.

### 원고를 쓴 뒤 — 교과서에 없는 내용이 들어갔는지 검사한다

파이프라인의 뒷구간(원고 → 산출물)만 대조하면 **내가 원고를 쓰면서 지어낸 것은 그대로 통과한다.**
실측(2026-09-04, 사회 5-1 03차시): 22장 원고에 교과서에 없는 정의가 4개 들어갔다
(집단 따돌림·사이버 폭력·사생활 침해·편견과 차별). 교과서는 그 넷을 나열만 하고 정의하지 않는다.

`verify_output.py` 의 `extra_sentences()` 를 **교과서를 정답 원본으로** 놓고 돌린다.

```bash
PYTHONUTF8=1 python -c "import sys; sys.path.insert(0,'scripts'); from verify_output import extract_pdf_text, extra_sentences; from pathlib import Path; 교과서 = extract_pdf_text('output/<차시>/01_차시.pdf'); [print(f.name, len(extra_sentences(교과서, f.read_text(encoding=\"utf-8\"))))  for f in sorted(Path('output/<차시>/00_원고').glob('*.md'))]"
```

| 정상 | 고칠 것 |
|---|---|
| 슬라이드·절 제목 | 교과서에 없는 **정의**나 **사실** |
| 동기유발의 장면 제목·도입 내레이션 | 교과서 문장을 바꿔 쓴 것 |
| 낱말 나열(`채집, 사냥, 고기잡이`) | |

실측(2026-09-07, 사회 5-2 02·03차시): 교과서 문장만 쓰자 형성평가는 0건, 슬라이드는 8건이
나왔고 그 8건이 전부 제목이었다. 지어낸 정의는 0건이었다.

## 4단계 — NotebookLM 조판

차시마다 새 노트북을 만든다. 소스 오염을 막기 위해 재사용하지 않는다.

```bash
notebooklm create "<차시 slug>"

# 교과서 차시 PDF 를 올린다. 슬라이드 생성의 소스다.
notebooklm source add output/<차시>/01_차시.pdf --notebook <id> --json

# 원고를 하나씩 올린다. source add 는 파일 하나만 받는다.
notebooklm source add output/<차시>/00_원고/01_동기유발.md --notebook <id> --json
notebooklm source add output/<차시>/00_원고/03_형성평가_학생용.md --notebook <id> --json
notebooklm source add output/<차시>/00_원고/04_개념정리.md --notebook <id> --json

# source_id 를 확인하고, 각각 처리 완료를 기다린다.
notebooklm source list --notebook <id> --json
notebooklm source wait <source_id> --notebook <id>
```

`source wait` 와 `artifact wait` 는 id 를 **위치 인자로** 받는다. `--notebook` 만
주고 id 를 빼면 rc=2 로 거부된다. 그래서 `source list` 가 먼저 와야 한다.

`scripts/nblm_run.py` 의 `build_generate_command()` 로 명령을 만들어 **4종을
동시에** 요청한다. 직렬이면 한 시간이 넘고 병렬이면 최장 하나만큼(약 45분)이다.

**형성평가 정답지는 NotebookLM 을 거치지 않는다.** 원고를 `quiz_to_html.py` 가 바로 조판한다.
이유는 아래 「형성평가 정답지는 왜 직접 조판하는가」 참고.

```bash
PYTHONUTF8=1 python -c "import sys; sys.path.insert(0,'scripts'); \
from nblm_run import build_generate_command; \
print(' '.join(build_generate_command('slide-deck', '<프롬프트>', '<id>', '<source_id>')))"
```

생성마다 `-s` 로 자기 원고 하나만 지정한다.

| 생성 | 소스 | 산출물 |
|---|---|---|
| video | `01_동기유발.md` | `02_동기유발.mp4` |
| slide-deck | **`01_차시.pdf`(교과서)** | `03_수업슬라이드.pdf` / `.pptx` |
| infographic | `03_형성평가_학생용.md` | `04_형성평가.png` (**학생 배부본**) |
| infographic | `04_개념정리.md` | `05_개념정리.png` (개념 정리) |

인포그래픽 생성이 두 번인 점에 주의한다. 소스를 잘못 지정하면 학생 문제지에 정답이 찍히거나,
개념 정리 자리에 문제지가 나온다. `-s` 를 반드시 확인한다.

### 프롬프트 골격 — 원고 소스 (동기유발·형성평가·인포그래픽)

```
이 노트북의 소스는 이미 완성된 수업 자료 원고입니다. 초안이 아닙니다.
초등학교 N학년 M학기 사회 수업 자료입니다.

[반드시 지킬 것]
- 원고의 모든 문장을 글자 하나 빠뜨리거나 바꾸지 말고 그대로 사용하세요.
- 원고에 없는 내용을 추가하지 마세요. 원고에 없는 상자·말풍선·문구를 만들지 마세요.
- 원고의 순서를 바꾸지 마세요.
- 고유명사와 용어(이 차시의 핵심 낱말을 나열)는 절대 다른 말로 바꾸지 마세요.
- 마크다운 기호를 화면에 쓰지 마세요. '#', '##', '*', '-' 는 서식 기호일 뿐입니다.
- 모든 글자는 한국어이며 맞춤법이 정확해야 합니다. 뜻을 알 수 없는 글자를 만들지 마세요.

[자유롭게 할 것]
- 색상, 서체, 여백, 아이콘, 삽화, 레이아웃, 시각적 강조
- 초등학생이 보기 좋은 밝고 친근한 디자인
```

용어 목록은 차시마다 다시 쓴다. 앞 차시 것을 그대로 두면 엉뚱한 낱말을 지키느라 힘을 쓴다.

### 프롬프트 골격 — 교과서 소스 (수업 슬라이드)

```
이 소스는 초등학교 N학년 M학기 사회 교과서의 한 차시(◯◯~◯◯쪽)입니다.
「<차시명>」 차시의 수업 슬라이드를 만들어 주세요.

[반드시 지킬 것]
- 교과서 본문의 문장을 그대로 사용하세요. 다시 쓰거나 요약하지 마세요.
- 교과서에 없는 내용을 추가하지 마세요.
- 교과서의 순서를 따르세요.
- 고유명사와 용어(이 차시의 핵심 낱말을 나열)는 절대 다른 말로 바꾸지 마세요.
- 마크다운 기호를 화면에 쓰지 마세요.
- 화면에 보이는 글자는 모두 한국어여야 합니다. 영어 낱말·문구·도장·라벨을
  장식으로도 넣지 마세요. 뜻을 알 수 없는 글자를 만들지 마세요.
- 같은 낱말을 두 번 이어서 찍지 마세요.

- 교과서의 '확인해요' 처럼 학생이 답을 고르거나 써야 하는 문항은 **답을 표시하지 마세요.**
  고르는 문항의 보기에 체크 표시나 강조를 넣지 말고, 빈칸은 비워 두세요.
- 그 시대에 없던 것을 그리지 마세요. 공룡, 현대의 물건, 다른 시대의 유물을 배경이나
  장식으로도 넣지 마세요.

[자유롭게 할 것]
- 이 덱으로 40분 한 차시 수업을 진행합니다. 그 분량에 맞게 충분히 만들어 주세요.
  교과서의 한 문단을 여러 장으로 나누어도 좋고, 활동과 발문을 별도의 장으로 두어도 좋습니다.
- 장수는 알아서 정하세요.
- 색상, 서체, 여백, 아이콘, 삽화, 레이아웃, 시각적 강조
- 초등학생이 보기 좋은 밝고 친근한 디자인. 한 덱 안에서 톤을 일관되게 유지하세요.
```

**장수를 숫자로 못박지 않고 "40분 한 차시 분량" 이라고만 준다.** 숫자를 주면 지키느라 내용을
늘리거나 줄이고, 아무 말도 안 하면 교과서 분량 그대로 짧게 나온다(실측 2026-09-07:
3쪽짜리 차시에 아무 지시 없이 11장 → 40분 수업에 모자람).

**답을 표시하지 말라는 줄이 필요한 이유**도 실측이다. 교과서의 `돌을 ( 깨뜨려 , 갈아 ) 만든`
같은 고르기 문항에서 **두 보기 모두에 체크 표시**가 붙어 나왔다. 학생이 풀 문항인데 답이
찍혔고, 게다가 둘 다 맞다고 표시되어 오답을 유도한다.

영어 장식과 톤 불일치를 막는 줄이 들어 있는 이유는 실측이다. 이 줄들이 없던 첫 실험에서
표지에 `JOURNAL ENTRY`·`PROPERTY OF: THE CHRONICLE EXPLORER`, 먹을거리 장에
`Gathering / Hunting / Fishing`, 활동 장에 `CLASSIFIED`·`TOP SECRET` 도장이 찍혔고,
모험 저널·귀여운 토끼·극비문서 톤이 한 덱에 섞였다. 낱말 중복도 1곳 났다.

### 다운로드

`generate ... --json` 은 `{"task_id": ..., "status": "pending"}` 을 돌려주고 바로 끝난다.
`artifact_id` 라는 키는 없다 — **`task_id` 가 곧 아티팩트 id** 다(`artifact list` 로 확인됨).
종류마다 하나씩 기다린다. 기본 timeout 이 300초라 슬라이드(실측 5분)·영상(실측 6.5분)은 넘긴다.

```bash
notebooklm artifact wait <task_id> --notebook <id> --timeout 3600 --interval 15 --json
```

다운로드는 간헐적으로 `[SSL: CERTIFICATE_VERIFY_FAILED] ... certificate key too weak` 로
실패한다. 항상은 아니고 15초 뒤 재시도하면 통과한다(실측: 인포그래픽·영상 모두 1회 실패, 2회 성공).
`nblm_run.run()` 이 SSL 오류만 3회 재시도한다. 직접 셸에서 받을 때도 한 번 실패로 판단하지 않는다.

`build_download_command(kind, out_path, notebook_id, fmt=None, artifact_id=None, force=False)`
로 받는다. 슬라이드는 PDF 와 PPTX 를 모두 받는다 — `fmt` 로 형식을 고른다.

**`artifact_id` 를 항상 준다.** 안 주면 CLI 는 완료된 것 중 최신을 받는데, 재생성 직후에는
새 아티팩트가 아직 pending 이라 이전 훼손본을 조용히 다시 받는다(실측 2회). 셸에서는
`-a <artifact_id>` 다. 같은 이름의 파일이 있으면 `--force` 없이는 `이름 (2).pdf` 로
옆에 저장되니 재생성 때는 `force=True` 로 덮어쓴다.

```bash
notebooklm download slide-deck output/<차시>/03_수업슬라이드.pdf --notebook <id>
notebooklm download slide-deck output/<차시>/03_수업슬라이드.pptx --format pptx --notebook <id>
notebooklm download video output/<차시>/02_동기유발.mp4 --notebook <id>
notebooklm download infographic output/<차시>/05_개념정리.png --notebook <id>
```

`--format` 은 `slide-deck`(pdf|pptx) 에만 있다. `video`·`infographic` 에 붙이면 거부된다.

### 형성평가 정답지 — NotebookLM 을 거치지 않는다

문항과 정답은 `03_형성평가.md` 에 이미 확정되어 있다. 조판만 하면 되므로 원고를 바로 PDF 로 만든다.

```bash
PYTHONUTF8=1 python scripts/quiz_to_html.py   output/<차시>/00_원고/03_형성평가.md output/<차시>/형성평가_인쇄용.html
PYTHONUTF8=1 python scripts/html_to_pdf.py   output/<차시>/형성평가_인쇄용.html output/<차시>/04_형성평가_정답.pdf
PYTHONUTF8=1 python scripts/html_to_pdf.py   output/<차시>/형성평가_인쇄용.html output/<차시>/04_형성평가_예비.pdf --hide-answers
```

`quiz_to_html.py` 는 문항과 정답의 **개수가 다르면 멈춘다.** 어긋난 채로 조판하면 4번 문제에
5번 답이 실린 정답지가 나오는데 종료 코드는 0 이라 아무도 모른다.

`--hide-answers` 는 `.answer` 요소를 숨긴다. 그 클래스가 하나도 없으면 PDF 를 만들지 않고
예외로 멈춘다 — 정답이 그대로 인쇄된 종이가 아이들에게 나가는 것보다 낫다.

**PDF 가 곧 원고라 대조가 필요 없다.** 예전에는 NotebookLM `generate quiz` 로 만든 뒤
`verify_output.py` 로 대조했는데, 그 단계가 있어서 대조가 필요했던 것이다.

#### 왜 뺐는가 (실측 2026-09-07)

quiz 생성은 내용을 더해 주지 않으면서 잃는 것만 있었다.

- 빈칸 채우기 문장을 **의문문으로 바꿔** 놓았다. `(   )이라고 한다` → `무엇이라고 합니까?`.
  배부본은 빈칸형이라 정답지와 문항 형태가 어긋났다.
- 원고에 없는 **4지선다 오답과 해설을 지어냈다.** 신석기 차시에서는 `고인돌`·`기와집` 같은
  다른 시대 유물과 그 설명이 붙었다. 대조에서 매번 "원고에 없는 문장" 으로 신고됐다.
- 레이트리밋을 한 번 더 썼다. 인포그래픽이 한도에 막혀 배부본을 못 만든 날, 이 한 번이 아깝다.

"정답지에서 기계 대조가 통과하면 배부본도 믿을 근거가 된다" 는 예전 논리는 약하다. 둘은 다른
생성기이고 다른 실행이다. 실제로 같은 차시에서 정답지는 멀쩡한데 배부본은 5번이 중복됐다.

### 형성평가는 왜 두 갈래인가

**학생에게 나가는 문제지는 NotebookLM 인포그래픽으로 만든다.** 텍스트만 조판한 종이가 아니라
디자인된 한 장이어야 초등학생이 집중한다. **정답지는 원고를 직접 조판한다.**

| 쓰임 | 만드는 법 | 대조 |
|---|---|---|
| 학생 배부본 | infographic ← `03_형성평가_학생용.md` | 사람이 문항을 확대해서 읽는다 |
| 교사 정답지 | `03_형성평가.md` → `quiz_to_html.py` → PDF | 필요 없다 (PDF 가 곧 원고) |
| 배부본 예비 | 같은 PDF 에서 `--hide-answers` | 필요 없다 |

**정답이 새지 않게 하는 방법은 소스 분리 하나뿐이다.** 프롬프트로 "정답을 넣지 마세요" 라고만 하면
막히지 않는다. `## 정답` 절이 없는 `03_형성평가_학생용.md` 를 **별도 소스로 올리고 `-s` 로 그것만
지정**한다. `03_형성평가.md` 는 노트북에 올리지 않는다.

프롬프트에 못박을 것 — "문항은 정확히 N개", "같은 문항을 두 번 넣지 말 것", "문장 앞부분을
잘라 내지 말 것", "정답·힌트·해설·보기·선택지를 넣지 말 것", "빈칸 괄호를 그대로 유지하고
학생이 연필로 쓸 수 있게 넉넉히 비울 것", "번호 1~N 을 순서대로", "답을 암시하는 그림·낱말 금지",
"그림은 작게 글자는 크게 — 문제지라 가독성이 그림보다 중요".

받는 것은 여느 인포그래픽과 같다.

```bash
notebooklm download infographic output/<차시>/04_형성평가.png   -a <artifact_id> --force --notebook <id>
```

결과는 이미지라 기계 대조가 안 되므로 **문항을 확대해 눈으로 읽는다**(실측 2026-09-04: 훼손 0.
2026-09-07 신석기 차시: 5번 문항이 두 번 나오고 한쪽은 문장 앞부분이 잘렸다 — 재생성 대상).

## 5단계 — 전수 대조

### 형성평가 정답지 — 대조하지 않는다

`04_형성평가_정답.pdf` 는 원고를 그대로 조판한 것이라 원고와 어긋날 수가 없다.
`quiz_to_html.py` 가 문항·정답 개수가 다르면 멈추므로 그 검사로 충분하다.

`verify_output.py` 는 이제 이 파이프라인에서 **원고 대 산출물** 대조에는 쓰지 않는다.
남은 쓰임은 3단계의 **교과서 대 원고** 검사 하나다.

### 형성평가 학생 배부본 — 사람이 봐야 한다

`04_형성평가.png` 은 이미지라 기계 대조가 안 된다. 확대해서 확인할 것 —
문항 수가 맞는가, 문장이 원고 그대로인가, 빈칸 괄호가 살아 있는가, **정답이나 선택지가
어디에도 없는가**, 번호가 순서대로인가. 아이들에게 나가는 종이이므로 정답 노출은 한 곳도 허용하지 않는다.

### 슬라이드·인포그래픽 — 사람이 봐야 한다

**기계 대조가 아예 안 된다.** 산출 PDF·PNG 는 페이지가 통째로 이미지라
텍스트 레이어가 없어서 추출하면 빈 문자열이 나온다. `verify_output.py` 를
슬라이드에 돌리면 "원고가 전부 누락" 으로 나오지 훼손을 가려내지 못한다.

PNG 로 렌더링해 Read 도구로 읽고 원고와 직접 대조한다.

```bash
PYTHONUTF8=1 python -c "import sys; sys.path.insert(0,'scripts'); \
from verify_output import render_pdf_pages; \
print(*render_pdf_pages('output/<차시>/03_수업슬라이드.pdf', 'output/<차시>/슬라이드_png'), sep='\n')"
```

읽을 때 특히 볼 것 — 낱말이 두 번 찍혔는지, 고유명사가 다른 말로 바뀌었는지
(`무신정변` → `무인들의 봉기`), 원고에 없는 문장이 끼어들었는지, 장수가 맞는지.

선례에서 실제 훼손이 난 곳이 슬라이드다(17장 중 2장). 여기서 눈이 유일한 검출기다.

인포그래픽은 한 장에 글자가 많아 더 자주 깨진다(실측: 1회차에 절 중복·문장 합침·
번호 어긋남·글자 깨짐이 한꺼번에 났고, "각 절은 정확히 한 번"·"번호 1~5 순서대로"·
"글자를 작게 하지 말고 그림을 줄여라" 를 넣은 2회차에 구조는 잡혔으나 `휠체어`→`휠제어`,
`맨`→`땐` 같은 한 획 오타는 남았다). 전체 이미지를 한 번 보고 끝내지 말고 **띠별로 잘라
원본 해상도로 확대해서** 읽는다. 축소본으로는 한 획 차이가 구분되지 않는다.

PPTX 도 장마다 이미지 한 장이다(실측: 텍스트 프레임 0개). 한 글자 오타를 PPTX 에서
고칠 수 없으므로 훼손이 한 곳이라도 있으면 통째로 재생성한다.

### 영상 — 대조하지 않는다

동기유발이 목적이라 문구가 조금 바뀌어도 수업에 지장이 없다.

### 리포트

결과를 `output/<차시>/99_대조리포트.md` 에 적는다. 자료별로 무엇을 어떻게
확인했는지, 슬라이드는 몇 장을 눈으로 봤는지 남긴다.

**위 분류에서 훼손으로 판정된 것이 있으면 해당 자료를 통째로 재생성한다.**
정상적인 재조판만 신고된 경우에는 재생성하지 않는다. `revise-slide` 는 쓰지 않는다 —
제자리 수정이 아니라 새 아티팩트로 분기하고, 문장을 통째로 바꿔 쓴다.

## 흐름 밖에 있는 것

`scripts/build_standards.py` 는 차시마다 도는 게 아니라 **최초 1회 데이터 구축**용이다.
2022 개정 교육과정에서 초등 3개 학년군을 추려 `data/standards-elementary.json`(611건)을 만든다.
이후에는 `find_standards.py` 가 그 파일만 읽는다. 교육과정이 개정되지 않는 한 다시 돌릴 일이 없다.

## 알려진 한계

이 세 가지는 도구로 막을 수 없다. 사용자에게 그대로 알린다.

**교과서 그림이 안 들어간다.** NotebookLM 은 소스에 박힌 이미지를 읽지도,
생성물에 원본 이미지를 넣지도 못한다. 교과서 사진·삽화·지도는 슬라이드에
들어가지 않는다. 그림 자체가 학습 자료인 차시(지도 읽기, 유물 관찰)에서는
교과서를 따로 띄워야 한다.

**슬라이드·인포그래픽은 기계 대조가 불가능하다.** 위 5단계 참고. 사람이 봐야 한다.

**인포그래픽 원고는 250자 안팎으로 쓴다.** 한 장에 글자가 많을수록 한 획 오타가 늘어난다.
실측(2026-09-07, 사회 5-2 02차시): 같은 차시에서 371자 원고는 오타 6곳, 그 상태로 틀린 낱말을
프롬프트에 하나씩 적었더니 오히려 13곳으로 늘었고, 원고를 247자로 줄이자 5곳으로 떨어졌다.
같은 날 247자짜리 형성평가 인포그래픽과 장당 30자 안팎인 슬라이드 20장은 오타가 0곳이었다.
글자를 작게 그리면서 생기는 문제이므로 프롬프트로는 줄지 않는다. **틀린 낱말을 프롬프트에
나열하는 것은 효과가 없다** — 그 낱말은 고쳐져도 다른 낱말이 깨진다. 줄일 것은 글자 수다.

**슬라이드 그림에 그 시대에 없던 것이 그려질 수 있다.** 실측(2026-09-07, 신석기 차시):
표지 배경에 공룡 세 마리가 그려졌다. 글자는 교과서 그대로인데 그림이 틀린 경우라, 문장만
읽으면 놓친다. 슬라이드를 볼 때 **글자뿐 아니라 그림도 그 시대의 것인지** 본다.
프롬프트에 금지 줄을 넣었으나 효과는 아직 확인되지 않았다.

**인포그래픽의 한글 한 획 오타는 재생성으로 0 이 되지 않을 수 있다.** 실측(2026-09-04, 사회 5-1 03차시):
4회 재생성에서 매번 다른 낱말 한 곳이 깨졌다(`휠제어`·`땐`·`보장맏지` → `칫즐` → `판안하고`). 틀린 낱말을
프롬프트에 명시하면 그 낱말은 고쳐지지만 다른 낱말이 깨진다. 3회쯤에서 멈추고, 남은 한 곳을 대조리포트에
적어 교사가 그림 편집기로 고치거나 말로 보완하게 한다. 슬라이드는 장당 글자가 적어 2회차에 0 이 됐다.

**원고 낱말만 재조합한 거짓말은 못 잡는다.** `단군왕검이 8조법을 로마에서
만들었다` 처럼 낱말은 다 원고에 있고 관계만 틀린 경우, 낱말 통계로는 구분되지
않는다. 형성평가에서도 이건 눈으로 봐야 한다.

### 레이트리밋

생성이 레이트리밋으로 실패하면 5~10분 후 해당 자료만 재요청한다.

**종류별로 따로 걸리고, 계정 전체에 걸린다.** 실측(2026-09-04): 슬라이드를 3번 만든 뒤 4번째가
막혔는데(`RATE_LIMITED: Slide Deck generation rate limited by Google`), 같은 시각 인포그래픽은
정상 생성됐다(인포그래픽은 5번까지 통과). **새 노트북을 만들어 원고만 올리고 시도해도 똑같이 막힌다** —
한도는 노트북이 아니라 계정에 걸려 있으므로 노트북을 바꿔 우회할 수 없다. 1시간 뒤에도 풀리지 않았다.

그래서 **슬라이드 원고는 처음부터 장수를 맞춰 쓴다.** 8장을 만들어 본 뒤 22장으로 다시 만드는 식이면
재생성 한도를 그 차시 하나에 다 써 버린다. 40분 수업이면 원고 단계에서 19장 이상으로 잡는다.

**진단은 `-v` 로 한다.** `-v` 는 하위 명령이 아니라 `notebooklm` 바로 뒤에 온다
(`notebooklm -v generate slide-deck ...`). 이것 없이는 stderr 에 `RPC CREATE_ARTIFACT failed`
한 줄만 남아 레이트리밋인지 다른 오류인지 구분되지 않는다.

**재시도에 `--retry` 를 쓰지 않는다.** CLI 내부 백오프가 1분·2분·4분이라 한 번 호출이 7분 넘게
잡혀 있고, 그동안 다른 일을 못 한다. `--retry 0` 으로 즉시 실패시키고 바깥에서 10분 간격으로
다시 요청하는 편이 낫다.
